overwrite: true
schema: "./server/schema.graphql"
documents: null
generates:
  ./server/src/api/graphql.ts:
    plugins:
      - "typescript"
      - "typescript-resolvers"
  ./graphql.schema.json:
    plugins:
      - "introspection"
