overwrite: true
schema: "./schema.graphql"
documents: null
generates:
  src/api/graphql.ts:
    plugins:
      - "typescript"
      - "typescript-resolvers"
  ./graphql.schema.json:
    plugins:
      - "introspection"
